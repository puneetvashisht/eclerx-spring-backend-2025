# Docker commands
    docker pull : pull image from repository
    docker images : see all images on local machine downloaded from registry (docker hub default)
    docker run : run the docker image - create a live container